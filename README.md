# Pomodoro Timer

Not a fan of the clock app on Mac so I buit something else.

## Run

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
python -m pomodoro
```

## Tests

```bash
python -m pytest
```
